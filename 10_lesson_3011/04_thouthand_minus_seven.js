let n = 1000;

while (n > 0) {
    console.log(n + ' - 7 = ' + (n -= 7))
}
console.log('Я гуль.')

/*
-=, +=, *=, /=, %= - это все операторы, которые
изменяют значение перемменной и сохраняют полученное
значение в ней же.

 -- <- это уменьшение на 1.
 -= n <- это уменьшение на n

*/
